// concat
[...arr1, ...arr2]
[1, 2, 3 ,2 , 4, 5]

arr.join(",")

//pop 先进后出 栈
var arr = [1, 2, 3]
arr.pop() //3
arr // 1, 2

// shift() 先进先出， 队列
arr.shift() //1
arr [2, 3, 4]

// push
arr.push(4)

// unshift()
arr.unshift(0);



//reverse()
[4, 3, 2, 1]



// slice 选中某个段落，返回新数组
arr.slice(0, 2);  //下标， 第几个数

// splice 改变数组，删除元素, 新增元素
arr.splice(index, howmany, item1, ...itemx)

arr.splice(1, )